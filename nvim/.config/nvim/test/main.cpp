#include "teszao.h"
#include <algorithm>
#include <iostream>

using std::fill;
using std::sort;

int main() {
  Teste bzao;
  bzao.aaa();
  int *a = new int(10);
  fill(a, a + 1, 1);

  sort(a, a + 10);
}
