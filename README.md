## My dotfiles 


Used stow to link the files

# To install, use the command:

```
git clone git@github.com:dodonut/dotfiles.git $HOME && \
        chmod +x $HOME/dotfiles/scripts/.config/scripts/install.sh && \
        $HOME/dotfiles/scripts/.config/scripts/install.sh
```
