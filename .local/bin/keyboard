#!/bin/bash

# shell scipt to prepend i3status with more stuff
case $BLOCK_BUTTON in
    1) setxkbmap us;;
	3) setxkbmap br;;
	2) notify-send "Keyboard layout" "\- Left for 'us' - Right for 'pt'"
esac
LG=$(setxkbmap -query | awk '/layout/{print $2}') 
printf "%s" $LG
